<%=  form_with url: user_post_path, model:[ @post, @user, @post.comments.build ] do |f| %>
  <div>
    <%= f.label 'Comment to this post:' %><br>
    <%= f.text_area :text %>
  </div>

  <div>
    <%= f.submit "Create comment" %><br>
  </div>
<% end %>